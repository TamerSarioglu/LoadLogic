package com.tamersarioglu.loadlogic.entity

/**
 * Enum representing user roles in the LoadLogic system.
 * Defines the three types of users: CHIEF (administrative), DRIVER (vehicle operator), and CREW (site worker).
 */
enum class Role {
    CHIEF,
    DRIVER,
    CREW
}